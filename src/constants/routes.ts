export enum AuthRoutes {
    SignIn = "/sign-in",
    SignUp = "/sign-up",
  }

  export enum AppRoutes {
    MyProfile = "/my-profile",
    Orders = "/orders",
    Wishlist = "/wishlist",
    Rewards = "/rewards",
  }